import type { ProviderConnectionDraft } from "@tali/contracts";
import { ProviderFormSection, ProviderTextField, ProviderTextareaField } from "./fields";
import type { ProviderConfiguratorProps } from "./types";
type Draft = Extract<ProviderConnectionDraft, { provider: "vertex-ai" }>;
export function VertexAIProvider({ disabled, errors, onChange, value }: ProviderConfiguratorProps<Draft>) { return <ProviderFormSection title="Google Cloud project"><div className="grid gap-4 sm:grid-cols-2"><ProviderTextField id="vertex-project" label="Project ID" required value={value.config.project} disabled={disabled} error={errors.project} onChange={(project) => onChange({ ...value, config: { ...value.config, project } })} /><ProviderTextField id="vertex-location" label="Location" required value={value.config.location} disabled={disabled} onChange={(location) => onChange({ ...value, config: { ...value.config, location } })} /></div><ProviderTextareaField id="vertex-service-account" label="Service account JSON" required value={value.credentials.serviceAccountJson} disabled={disabled} error={errors.serviceAccountJson} placeholder="Paste the complete service-account JSON document" onChange={(serviceAccountJson) => onChange({ ...value, credentials: { serviceAccountJson } })} /></ProviderFormSection>; }

# Placeholder-only fragment. Merge these keys into the complete Secret named by
# secrets.existingSecret; never commit rendered or real credential values.
apiVersion: v1
kind: Secret
metadata:
  name: tali-relay-secrets
type: Opaque
stringData:
  metrics-token: REPLACE_WITH_AN_INDEPENDENT_RANDOM_METRICS_TOKEN
  hindsight-database-password: REPLACE_WITH_A_RANDOM_DATABASE_PASSWORD
  hindsight-database-url: postgresql://hindsight:REPLACE_WITH_URL_ENCODED_PASSWORD@tali-relay-postgresql:5432/hindsight
  hindsight-api-key: REPLACE_WITH_A_RANDOM_INTERNAL_API_KEY
  hindsight-router-token: REPLACE_WITH_A_DIFFERENT_RANDOM_INTERNAL_ROUTER_TOKEN

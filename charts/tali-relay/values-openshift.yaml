# OpenShift profile for clusters where the administrator permits anyuid.
#
# This profile configures the Control namespace. Business OpenShell SCC grants
# are managed separately by the platform in each Project Namespace.
openshift:
  enabled: true
  anyuidScc:
    createRoleBinding: true
  routes:
    control:
      enabled: true

control:
  service:
    type: ClusterIP

keycloak:
  service:
    type: ClusterIP

runner:
  projectTargetRouting:
    serviceProxy:
      type: ClusterIP

agentSandbox:
  podSecurityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault
